import { getTestInstance } from '../tests/utils';
import Mutation from '../src/resolvers/Mutation';

describe('Resolver - Mutation', () => {
  it('should pass the test', () => {
    expect(1).toEqual(1);
  });
});
